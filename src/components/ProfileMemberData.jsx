const React = require('react');

var ProfileMemberData = React.createClass({
  render: function () {
    return (
      <h1>Profile Member Data</h1>
    );
  }
});

module.exports = ProfileMemberData;
